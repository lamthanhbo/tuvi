services:
  - type: web
    name: tuvi-grok-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GROK_API_KEY
        sync: false